# Base URL of the ReadMatch AI backend API. Defaults to
# http://localhost:8000 (see src/lib/config.ts) if unset -- this file only
# needs to be copied to .env.local when pointing at a different backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
